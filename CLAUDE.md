# bead-swarm

Read `AGENTS.md`. Use the `bead-swarm` skill.

Do not empty a bead graph in this chat. Exec `./bin/bead-swarm` (per-repo) or `bead-swarm` (global shim). Never copy `bin/` off this checkout.
