# bead-swarm

Read `README.md` then `AGENTS.md`. Use the `bead-swarm` skill.

The product is `bin/bead-swarm`. Do not empty a bead graph in this chat. Exec `./bin/bead-swarm` (per-repo) or `bead-swarm` (global shim). Never copy `bin/` off this checkout.
